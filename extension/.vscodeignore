# Source TypeScript — compiled output in dist/ is what runs
src/
mcp-server/
tsconfig.json

# node_modules — MCP server is bundled via esbuild into dist/mcp-server/server.js
node_modules/

# Test infrastructure
**/*.test.js
**/*.spec.js
jest.config.*

# Packaging artefacts — don't bundle old VSIXes inside the new one
*.vsix

# Dev tooling
.vscode/
.eslintrc*
.prettierrc*
